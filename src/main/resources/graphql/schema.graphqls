type User {
    userId: ID!
    username: String!
    password: String
}

input NewUserInput {
    username: String!
    password: String
}

type Query {
    getUserById(userId: ID!): User
}

type Mutation {
    createUser(user: NewUserInput!): User
}